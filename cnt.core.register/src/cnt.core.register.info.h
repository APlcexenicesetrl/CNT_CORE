#pragma once

#include "cnt.core.register.win64.h"

struct {
	struct {
		struct {
			struct {

			} lang;
			
			struct {
				std::vector<LPCWSTR> commandList;
			} list;
		} registers;
	} core;
} cnts;